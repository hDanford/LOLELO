"""Data-source adapters. Each turns a raw source into a list[Match].

The engine doesn't care where matches come from -- add an adapter here for
Oracle's Elixir, Leaguepedia, or anything else, and the rest is unchanged.
"""
